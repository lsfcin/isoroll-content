# Loop 4b — postproc-tests

## Carry
slug: postproc-tests | branch: feature/postproc-tests (base: feature/f1-procedural-spine — F1 core not yet merged to develop) | root: /mnt/workspace/code/isoroll-content
test-cmd: `/mnt/workspace/.venv/bin/python3 -m pytest test/ -q` | e2e-cmd: none
criticality: normal | verdict: standard
criteria: C1 — pytest suite exists, green, covering sheet_grid.detect_grid + strip_linework on synthetic magenta-grid fixtures
  C2 — guide_marks.residue_count tested: synthetic image with K cyan symbols → count in expected band; 0 on clean image
  C3 — new silhouette-IoU QC function (own file ≤200 LOC) comparing an NB output cell's silhouette vs the guide box mask; unit tests: ≈1.0 identical masks, <0.5 disjoint
  C4 — sheet_postproc split tolerates ±2px grid-line drift on a synthetic sheet (test proves it)
  C5 — `make verify-fast` still green; every file ≤200 LOC; no NB/API calls in tests
tasks: T1 — pytest bootstrap (pytest.ini + conftest sys.path src/cli+src/pipeline + fixtures.py) — pytest.ini,test/conftest.py,test/fixtures.py — medium
  T2 — test detect_grid+strip_linework (C1) — test/test_sheet_grid.py — medium
  T3 — test ±2px drift snap (C4) — test/test_grid_drift.py — medium
  T4 — test residue_count (C2) — test/test_guide_marks.py — medium
  T5 — new QC module silhouette_iou + tests (C3) — src/cli/sheet_qc.py,test/test_sheet_qc.py — medium
  T6 — wire pytest into verify-fast, full gate green (C5), harden split only if T3 red — Makefile,src/cli/sheet_postproc.py(if needed) — medium
context: /mnt/workspace/code/isoroll-content/CONTEXT.md, src/cli/CONTEXT.md, src/pipeline/CONTEXT.md, ROADMAP-content-gen.md

## Code

Worktree: `/tmp/claude-1000/-mnt-workspace/bb9b9715-4ea6-4628-9115-ce47ee08dba4/scratchpad/wt-postproc-tests` (branch feature/postproc-tests). Starting state: Loop 4a's red commit (`01490d4`) — 7 tests green (T1–T4), `test/test_sheet_qc.py` red with `ModuleNotFoundError: No module named 'sheet_qc'`.

T5: wrote `src/cli/sheet_qc.py` (46 LOC) exactly per 3-arch.md's pinned decisions — no re-guessing: `ALPHA_MIN = 8`; `silhouette_mask(img, alpha_min=ALPHA_MIN)` thresholds the alpha channel when the image carries transparency (`RGBA`/`LA`/`PA` mode or a `transparency` info key), else falls back to a luminance threshold (`LUMA_MIN = 16`, not pinned by arch — chosen as an "not near-black background" floor, exercised by no test since both fixtures are RGBA); `mask_iou(a, b)` = `|a∧b| / |a∨b|` via `ImageChops.multiply` (AND) / `ImageChops.lighter` (OR) on binary L masks — reusing the exact AND/OR-via-ImageChops idiom already used in `sheet_grid.py`'s `line_mask`/`magenta_mask` compositing, and the `sum(1 for p in mask.getdata() if p)` pixel-count idiom already used in `guide_marks.residue_count` — no copy-paste, same two one-line idioms applied to a new mask pair; union==0 → 1.0 per spec (avoids /0, and correctly scores two empty silhouettes as a perfect match rather than a mismatch); `silhouette_iou(cell, box_mask, alpha_min=ALPHA_MIN)` composes the two. `.pyi` auto-generated by the commit-time stubgen hook (46 lines, matches source).

attempt 1: T5 (sheet_qc.py) → 0 red, all 8 tests pass (`8 passed, 14 warnings in 0.02s`, warnings are pre-existing Pillow `getdata` deprecations in sheet_grid.py/guide_marks.py/test files, not introduced here)

T6: Makefile — appended `$(PY) -m pytest test/ -q` to both `verify-fast` and `verify-full` after the existing `compileall` line, per arch. No `sheet_postproc.py` edit: C4's drift test (`test_grid_drift.py`) was already green in the T1–T4 pass (attempt 1, no red run against it) — `detect_boundaries`' search window (`max(2, cell*0.25)` = 25px on a 100px test cell) already exceeds the ±2px drift fixture, exactly as 3-arch.md predicted, so T6's conditional hardening clause did not trigger.

green: yes run: `8 passed, 14 warnings in 0.02s`
`make verify-fast` (post-commit): `✓ verify:fast green` (compileall + pytest, pre-commit hook output)

touched: src/cli/sheet_qc.py (new, 46 LOC), src/cli/sheet_qc.pyi (new, auto-generated stub), Makefile (verify-fast/verify-full now run pytest), src/cli/CONTEXT.md (auto-synced routing-table row for sheet_qc.py, hook-generated)

Committed on `feature/postproc-tests` in the worktree: commit `ec7608d` — "feat(cli): sheet_qc silhouette IoU + wire pytest into verify gate". Pre-commit hook ran clean: no file over the 200-LOC threshold, `verify:fast` green, routing-sync applied, one non-blocking advisory (`⚠ src/cli: 13 code files — consider splitting`, unrelated to this change, pre-existing directory-size note).

No test was edited to make it pass — `test/test_sheet_qc.py` (from Loop 4a) is untouched; only the missing `sheet_qc` module was written, matching its expectations exactly on the first attempt.

executor: loop-medium model=sonnet tier=medium
