# ── Curated reference set — fully versioned, no exceptions needed ────────────
# benchmarks/ (top-level) holds curated comparison images + manifests.

chars/
output/
temp/
*.zip

# ── Python / runtime noise ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# ── Local machine config ──────────────────────────────────────────────────────
.env
.env.*
*.log
