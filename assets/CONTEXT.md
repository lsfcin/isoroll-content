# assets
> Generated art output — characters and tiles. Not curated reference material (see `../benchmarks/` for that).

`chars/` is gitignored (raw generation output, regenerate via the pipeline). `tiles/` is tracked — promoted tile art lives here directly, no separate curation step yet.

<!-- routing:start -->
## Routing

<!-- routing:end -->
